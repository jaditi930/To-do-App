import { Router, Request, Response } from "express";
import expressAsyncHandler from "express-async-handler";


const router = Router();

router.get("/add",expressAsyncHandler(async (req:Request,res:Response)=>{
    
}))





module.exports=router
